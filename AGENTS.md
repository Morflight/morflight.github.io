# morflight.github.io

Codex entry point for this project.

Read `.ai/README.md` first, then `CLAUDE.md` for the current project overview and operating constraints. This is a GitHub Pages/static site project; preserve the no-unnecessary-build-step posture unless project docs say otherwise.

Treat `.claude/settings*.json` as legacy Claude configuration only.
## Codex Standards

This project follows: `codex-github-pages`. Read the matching Codex plugin skill/reference before changing project-type conventions, scaffolding, Docker, CI, secrets, deployment, or architecture covered by that standard.

## Shared AI Docs

Read `.ai/README.md` first for harness-neutral project knowledge. Prefer `.ai/*.md` over duplicated legacy `.claude/*.md` docs when both exist. Keep `.claude/**` for Claude-native commands, settings, hooks, skills, and legacy material.
